/**
 * Returns the sum of two numbers.
 *
 * @param x - The first number.
 * @param y - The second number.
 * @returns The arithmentic sum of `x` and `y`.
 */
export function add(x: number, y: number): number {
    return x + y;
  }
  
  export default add;
  